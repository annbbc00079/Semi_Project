

{% extends 'adbase.html.twig' %}

{% block title %}Order detail manager
{% endblock %}

{% block body %}

        <!-- Begin Page Content -->
<div calss="container-fluid ">
    <div class="card shadow mb-4">     
        <div class="card-body">
            <div class="table-reponsive">
            <table class="table table-bordered" id="database" with="100%" cellspacing="0">
            <thead>
            <tr>
                
                <th>Product name</th>
                <th>Product price</th>
                <th>Quantity</th>
                <th>Sumary</th>
                
            </tr>
            </thead>
            <tbody>
                {% for item in cart_manager.items %}
            <tr>
                <td>{{item.product.id}}
                    -
                    {{item.product.name}}</td>
                <td>{{item.product.price}}</td>
                <td>
                    {{item.quantity}}
                </td>
                <td>{{item.amount}} $</td>
            </form>
        </tr>
    {% endfor %}
    <tr>
        <td colspan="3" class="text-center">Total</td>
        <td>{{cart_manager.amount}}</td>
    </tr>
</tbody>

</table>
<td>
    <a href="{{path('app_ds_order')}}" class="btn btn-success">Back to List Order</a>
</td>
<br>
<br>
</div>
</div>
</div>
</div>
{% endblock %}
{% block javascripts %}
<script>
function deletesp(url){
    if(confirm("Do you delete product?")){
        window.location.href = url;
    }
}
</script>
<script>
    function Logout(url){
        if(confirm("Do you want to sign out?")){
            window.location.href = url;
        }
    }
    </script>
<script src="vendor/jquery/jquery.min.js"></script>
<script src="vendor/bootstrap/js/bootstrap.bundle.min.js"></script>

{% endblock %}
{% block stylesheets %}
    <link rel="stylesheet" href="{{asset('vendor/fontawesome-free/css/all.min.css')}}">
    <link rel="stylesheet" href="{{asset('css/admin.css')}}">
    <link rel="stylesheet" href="{{asset('https://fonts.googleapis.com/css?family=Nunito:200,200i,300,300i,400,400i,600,600i,700,700i,800,800i,900,900i')}}">
{% endblock %}
